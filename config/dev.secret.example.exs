use Mix.Config

# config :booru_box, BooruBox.Repo,
  # username: "username",
  # password: "password",
  # hostname: "localhost",
  # port: 5432,
  # database: "booru_box_dev",
  #socket_dir: "/tmp" # If connecting by unix socket
